<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.github.markpollack</groupId>
        <artifactId>agent-journal-parent</artifactId>
        <version>1.8.0</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>claude-code-capture</artifactId>
    <packaging>jar</packaging>

    <name>Claude Code Capture</name>
    <description>Claude Code SDK to journal-core bridge: phase capture, session parsing, and base run recording</description>

    <properties>
        <!-- claude-code-sdk publishes Java 21 bytecode (class file 65), so this module
             cannot be compiled or run on 17 whatever the parent declares. journal-core
             keeps the 17 baseline; only the modules that bind a 21 SDK move up. -->
        <java.version>21</java.version>
    </properties>

    <dependencies>
        <!-- Journal Core -->
        <dependency>
            <groupId>io.github.markpollack</groupId>
            <artifactId>journal-core</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Claude Code SDK (for ParsedMessage, SDK types) -->
        <dependency>
            <groupId>io.github.markpollack</groupId>
            <artifactId>claude-code-sdk</artifactId>
            <version>${claude-code-sdk.version}</version>
        </dependency>

        <!-- JSON processing (TraceWriter line serialization; version managed by parent) -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>

        <!-- Jackson 3 enters through claude-code-sdk -> MCP. These direct declarations
             publish the parent's managed floor to standalone consumers. -->
        <dependency>
            <groupId>tools.jackson.core</groupId>
            <artifactId>jackson-core</artifactId>
        </dependency>
        <dependency>
            <groupId>tools.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>
        <dependency>
            <groupId>tools.jackson.dataformat</groupId>
            <artifactId>jackson-dataformat-yaml</artifactId>
        </dependency>

        <!-- Logging -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>

        <!-- Test dependencies -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
